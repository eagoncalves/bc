import 'package:flutter/material.dart';

class PerformAppointment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text('Perfom Appointment'),
      ),
    );
  }
}
